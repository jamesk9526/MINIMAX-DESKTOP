import { useEffect, useMemo, useState } from 'react'
import { Check, CircleStop, ImagePlus, LoaderCircle, Plus, Shirt, Sparkles, Trash2, X } from 'lucide-react'
import { choices, type ObjectInfo } from '../lib/comfyInfo'
import { loadWardrobeProjects, newWardrobeProject, saveWardrobeProjects, wardrobeReferences, WARDROBE_LIBRARY_EVENT } from '../lib/wardrobeLibrary'
import { buildZImage } from '../lib/zimage'
import type { AppSettings, MediaFile, WardrobeProject } from '../types'
import { ReferenceApprovalModal } from './ReferenceApprovalModal'
import { analyzeReferenceImage } from '../lib/referenceAnalysis'

export function WardrobeStudio({ settings, info, connected, onNotice }: { settings: AppSettings; info: ObjectInfo; connected: boolean; onNotice(tone: 'error' | 'success' | 'neutral', text: string): void }) {
  const initial = useMemo(() => { const saved = loadWardrobeProjects(); return saved.length ? saved : [newWardrobeProject()] }, [])
  const [projects, setProjects] = useState(initial)
  const [activeId, setActiveId] = useState(initial[0].id)
  const [job, setJob] = useState<{ id: string; url: string; wardrobeId: string } | null>(null)
  const [busy, setBusy] = useState(false)
  const [message, setMessage] = useState('')
  const [error, setError] = useState(false)
  const [candidate, setCandidate] = useState<{ wardrobeId: string; file: MediaFile } | null>(null)
  const active = projects.find((project) => project.id === activeId) ?? projects[0]
  const selected = wardrobeReferences(active)
  const commit = (next: WardrobeProject[]) => { setProjects(next); saveWardrobeProjects(next) }
  const patch = (change: Partial<WardrobeProject>) => commit(projects.map((project) => project.id === active.id ? { ...project, ...change, updatedAt: Date.now() } : project))
  const patchById = (id: string, change: Partial<WardrobeProject>) => setProjects((current) => { const next = current.map((project) => project.id === id ? { ...project, ...change, updatedAt: Date.now() } : project); saveWardrobeProjects(next); return next })
  useEffect(() => { const refresh = () => { const next = loadWardrobeProjects(); if (next.length) setProjects(next) }; window.addEventListener(WARDROBE_LIBRARY_EVENT, refresh); return () => window.removeEventListener(WARDROBE_LIBRARY_EVENT, refresh) }, [])

  const renderPrompt = active.referencePrompt.trim() || [
    `Single-image three-view wardrobe reference for ${active.name}.`, active.description,
    active.colors && `Colors: ${active.colors}.`, active.materials && `Materials and construction: ${active.materials}.`,
    `${active.visualStyle}. Create one uninterrupted wide studio composition containing exactly three equally sized, evenly spaced, full-length headless neutral dress forms in one horizontal row. Show the exact same complete outfit from three distinct angles only: left figure straight front view, center figure three-quarter front view, right figure straight back view. Each figure wears one complete copy of the outfit. Keep every garment, layer, hem, closure, color, pattern placement, material, fit, proportion, and item of footwear identical across all three views. Keep all three outfits fully visible from neckline through footwear with generous space around them; do not crop any garment or shoe. Plain warm-neutral seamless studio backdrop, level eye-height camera, orthographic catalog presentation, soft even lighting, sharp fabric texture and construction detail. No grid, no quadrants, no panels, no borders, no divider lines, no inset images, no repeated angle, no side-profile view, no extra figure, no person identity, no face, no hair, no jewelry, no accessories, no handheld props, no text, no labels, no unintended objects, and no logos unless specified.`,
  ].filter(Boolean).join(' ')
  const zModel = choices(info, 'UNETLoader', 'unet_name').find((name) => /z[_-]?image.*turbo/i.test(name)) ?? 'z_image_turbo_bf16.safetensors'
  const zEncoder = choices(info, 'CLIPLoader', 'clip_name').find((name) => /qwen[_-]?3[_-]?4b/i.test(name)) ?? 'qwen_3_4b.safetensors'
  const zVae = choices(info, 'VAELoader', 'vae_name').find((name) => /^ae\.safetensors$/i.test(name)) ?? 'ae.safetensors'
  const zReady = connected && choices(info, 'UNETLoader', 'unet_name').includes(zModel) && choices(info, 'CLIPLoader', 'clip_name').includes(zEncoder) && choices(info, 'VAELoader', 'vae_name').includes(zVae)
  const chooseImage = async () => { const picked = await window.minimax.chooseMedia('image'); if (!picked) return; const projectId = active.id; const file: MediaFile = { ...picked, kind: 'image', preview: await window.minimax.mediaUrl(picked.path) }; patch({ referenceImages: [...active.referenceImages, file], selectedReferencePaths: [...new Set([...(active.selectedReferencePaths ?? active.referenceImages.map((item) => item.path)), file.path])] }); onNotice('neutral', 'Image added. The local vision model is filling empty wardrobe fields…'); try { const result = await analyzeReferenceImage(settings, picked.path, 'wardrobe'); const current = loadWardrobeProjects().find((item) => item.id === projectId); if (!current) return; patchById(projectId, { name: !current.name.trim() || /^Wardrobe \d+$/i.test(current.name) ? result.name || current.name : current.name, description: current.description.trim() ? current.description : result.description, colors: current.colors.trim() ? current.colors : result.colors, materials: current.materials.trim() ? current.materials : result.materials, visualStyle: current.visualStyle.trim() && current.visualStyle !== 'cinematic photorealism' ? current.visualStyle : result.visualStyle || current.visualStyle }); onNotice('success', 'Existing image analyzed; empty wardrobe fields were filled for review.') } catch (reason) { onNotice('error', `The image was added, but automatic description failed: ${reason instanceof Error ? reason.message : String(reason)}`) } }
  const createReference = async () => {
    if (!zReady || busy || !renderPrompt.trim()) return
    setBusy(true); setError(false); setMessage('Submitting the three-view outfit sheet to Z-Image Turbo…')
    try { const response = await window.minimax.submitPrompt(settings.comfyUrl, buildZImage(renderPrompt, 1024, 1024, Math.floor(Math.random() * 1_000_000_000), zModel, zEncoder, zVae)); setJob({ id: response.prompt_id, url: settings.comfyUrl, wardrobeId: active.id }); setMessage('Constructing the wardrobe sheet in ComfyUI…') }
    catch (reason) { setMessage(reason instanceof Error ? reason.message : String(reason)); setError(true); setBusy(false) }
  }
  const cancel = async () => { if (!job) return; try { await window.minimax.cancelPrompt(job.url, job.id); setMessage('Wardrobe render cancelled.') } catch (reason) { setMessage(reason instanceof Error ? reason.message : String(reason)); setError(true) } finally { setJob(null); setBusy(false) } }
  useEffect(() => {
    if (!job) return
    let disposed = false; let timer: ReturnType<typeof setTimeout>
    const poll = async () => {
      try {
        const history = await window.minimax.getHistory(job.url, job.id)
        const entry = history[job.id] as { status?: { status_str?: string }; outputs?: Record<string, { images?: Array<{ filename: string; subfolder?: string; type?: string }> }> } | undefined
        if (entry?.status?.status_str === 'error') throw new Error('Wardrobe generation failed. Check the ComfyUI log.')
        const image = Object.values(entry?.outputs ?? {}).flatMap((output) => output.images ?? [])[0]
        if (image) { const preview = await window.minimax.getOutputImage(job.url, image); const saved = await window.minimax.saveComfyOutputImage(job.url, image, settings.outputDirectory); if (!disposed) { setCandidate({ wardrobeId: job.wardrobeId, file: { ...saved, preview, kind: 'image' } }); setBusy(false); setJob(null); setMessage('Outfit sheet ready for approval.') }; return }
      } catch (reason) { if (!disposed) { setMessage(reason instanceof Error ? reason.message : String(reason)); setError(true); setBusy(false); setJob(null) }; return }
      timer = setTimeout(poll, 2000)
    }
    void poll(); return () => { disposed = true; clearTimeout(timer) }
  }, [job, settings.outputDirectory])
  const add = () => { const project = newWardrobeProject(projects.length + 1); commit([...projects, project]); setActiveId(project.id) }
  const remove = () => { if (!window.confirm(`Delete wardrobe “${active.name}”? Image files remain on disk.`)) return; const next = projects.filter((project) => project.id !== active.id); const fallback = next.length ? next : [newWardrobeProject()]; commit(fallback); setActiveId(fallback[0].id) }
  const approveCandidate = () => { if (!candidate) return; const project = loadWardrobeProjects().find((item) => item.id === candidate.wardrobeId); if (!project) return; patchById(project.id, { referenceImages: [...project.referenceImages, candidate.file], selectedReferencePaths: [...new Set([...(project.selectedReferencePaths ?? project.referenceImages.map((item) => item.path)), candidate.file.path])] }); setCandidate(null); setMessage('Wardrobe sheet approved.'); onNotice('success', 'Outfit sheet added to the approved wardrobe references.') }

  return <><div className="standard-page wardrobe-studio"><div className="page-heading"><div><p className="eyebrow">GLOBAL WARDROBE LIBRARY</p><h1>Wardrobe Studio</h1><p>Create reusable outfits and footwear. Jewelry, glasses, watches, bags, and recurring props live in Accessory Studio.</p></div><div className="heading-state"><span className={selected.length ? 'ok' : 'warn'}>{selected.length ? <Check size={15} /> : <Shirt size={15} />}{selected.length ? `${selected.length} approved reference${selected.length === 1 ? '' : 's'}` : 'Create an outfit reference'}</span></div></div><div className="character-studio-grid"><aside className="character-project-list"><header><span><strong>Wardrobes</strong><small>{projects.length} saved globally</small></span><button onClick={add}><Plus size={14} />New</button></header><div>{projects.map((project) => <button key={project.id} className={project.id === active.id ? 'active' : ''} onClick={() => setActiveId(project.id)}>{project.referenceImages[0]?.preview ? <img src={project.referenceImages[0].preview} alt="" /> : <span><Shirt size={18} /></span>}<span><strong>{project.name}</strong><small>{wardrobeReferences(project).length} approved outfit reference{wardrobeReferences(project).length === 1 ? '' : 's'}</small></span></button>)}</div></aside><section className="character-workbench"><header><div><Shirt size={18} /><span><strong>Wardrobe design</strong><small>Garments and footwear only; item references remain unambiguous.</small></span></div><button className="danger-button" onClick={remove}><Trash2 size={14} />Delete wardrobe</button></header><div className="wardrobe-workspace"><section className="character-setup-panel"><div className="character-section-heading"><span><Shirt size={15} /></span><div><strong>Outfit profile</strong><small>Describe one complete repeatable look.</small></div></div><div className="character-form"><label>Wardrobe name<input value={active.name} onChange={(event) => patch({ name: event.target.value, referencePrompt: '' })} /></label><label>Visual style<input value={active.visualStyle} onChange={(event) => patch({ visualStyle: event.target.value, referencePrompt: '' })} /></label><label className="wide">Garments and footwear<textarea value={active.description} onChange={(event) => patch({ description: event.target.value, referencePrompt: '' })} placeholder="Jacket, shirt, trousers, dress, shoes, fit and construction…" /></label><label>Colors and pattern<textarea value={active.colors} onChange={(event) => patch({ colors: event.target.value, referencePrompt: '' })} /></label><label>Materials and construction<textarea value={active.materials} onChange={(event) => patch({ materials: event.target.value, referencePrompt: '' })} /></label></div><div className="wardrobe-accessory-moved"><span><strong>Need a watch, necklace, glasses, bag, or prop?</strong><small>Create it in Accessory Studio as one isolated image, then assign it to a character.</small></span></div></section><section className="character-setup-panel wardrobe-render-panel"><div className="character-section-heading"><span><Sparkles size={15} /></span><div><strong>Z-Image outfit sheet</strong><small>Three consistent garment views in one image, without accessories or character identity.</small></div><em>{zReady ? 'Ready' : 'Unavailable'}</em></div><label className="character-reference-prompt"><span><strong>Editable render prompt</strong><small>One row: front, three-quarter, and back. Accessories are excluded.</small></span><textarea value={renderPrompt} onChange={(event) => patch({ referencePrompt: event.target.value })} /></label>{message && <div className={`character-master-message ${error ? 'error' : ''}`}>{busy && <LoaderCircle className="spin" size={13} />}<span>{message}</span></div>}<footer><button className="secondary-button" disabled={busy} onClick={() => void chooseImage()}><ImagePlus size={14} />Add existing</button>{busy && <button className="danger-button" onClick={() => void cancel()}><CircleStop size={14} />Cancel</button>}<button className="primary-button" disabled={busy || !zReady || !active.name.trim()} onClick={() => void createReference()}><Sparkles size={14} />{busy ? 'Creating…' : 'Create 3-view outfit'}</button></footer></section></div><section className="character-reference-set wardrobe-reference-set"><header><span><strong>Approved wardrobe references</strong><small>Selected outfit sheets are allocated after character identity pictures.</small></span><button className="secondary-button" onClick={() => void chooseImage()}><ImagePlus size={14} />Add image</button></header><div className="character-reference-grid">{active.referenceImages.map((file, index) => { const checked = active.selectedReferencePaths === undefined || active.selectedReferencePaths.includes(file.path); return <figure className={checked ? 'selected' : ''} key={`${file.path}-${index}`}><img src={file.preview} alt={`${active.name} wardrobe reference ${index + 1}`} /><figcaption><label><input type="checkbox" checked={checked} onChange={(event) => { const current = active.selectedReferencePaths ?? active.referenceImages.map((item) => item.path); patch({ selectedReferencePaths: event.target.checked ? [...new Set([...current, file.path])] : current.filter((path) => path !== file.path) }) }} />Use image {index + 1}</label></figcaption><button aria-label={`Remove wardrobe reference ${index + 1}`} onClick={() => patch({ referenceImages: active.referenceImages.filter((_, itemIndex) => itemIndex !== index), selectedReferencePaths: active.selectedReferencePaths?.filter((path) => path !== file.path) })}><X size={13} /></button></figure> })}<button className="character-add-reference" onClick={() => void chooseImage()}><ImagePlus size={19} /><span>Add reference</span></button></div></section></section></div></div>{candidate && <ReferenceApprovalModal title="Approve wardrobe outfit sheet" description="Check the front, three-quarter, and back views for matching construction, colors, materials, and footwear. No accessories should appear." image={candidate.file.preview ?? ''} approveLabel="Approve wardrobe" nextStep="Approval adds this outfit sheet to the selected wardrobe references." onClose={() => setCandidate(null)} onRetry={() => { setCandidate(null); void createReference() }} onApprove={approveCandidate} />}</>
}
